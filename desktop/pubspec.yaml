name: desktop
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.1
  path: ^1.8.3
  json_annotation: ^4.8.1
  provider: ^6.1.1
  url_launcher: ^6.2.5
  file_picker: ^8.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.7.1
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  windows:
    generate: true
    image_path: "res/icon/icon512.png"
    icon_size: 256

flutter:
  uses-material-design: true
